[[source]]
url = "https://pypi.python.org/simple"
verify_ssl = true

[packages]
litestar = ">=2.0.0"
uvicorn = {extras = ["standard"], version = ">=0.24.0"}
brotli = ">=1.1.0"
pydantic = ">=2.0.0"
jinja2 = ">=3.1.0"
werkzeug = ">=3.0.0"

[dev-packages]
rope = "*"
